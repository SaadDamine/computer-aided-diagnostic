INTRODUCTION

	La détection d'objets est une technologie qui détecte les objets sémantiques d'une classe dans les images et vidéos numériques.
	L'une de ses applications en temps réel est l'autopilotage des voitures.
	En cela, notre tâche consiste à détecter de multiples objets à partir d'une image.
	L'objet le plus courant à détecter dans cette application est la voiture, la moto et le piéton.
	Pour localiser les objets dans l'image, nous utilisons la localisation d'objet et devons localiser plus d'un objet dans les systèmes en temps réel.
	Il existe différentes techniques de détection d'objets, elles peuvent être divisées en deux catégories, la première est celle des algorithmes basés sur les classifications.
	CNN et RNN font partie de cette catégorie.
	Pour cela, nous devons sélectionner les régions intéressées dans l'image et les classer à l'aide du réseau neuronal convolutif.
	Cette méthode est très lente car nous devons effectuer une prédiction pour chaque région sélectionnée.
	La deuxième catégorie est celle des algorithmes basés sur les régressions.
	La méthode YOLO fait partie de cette catégorie.
Dans ce cas, nous ne sélectionnerons pas les régions intéressées à partir de l'image.
	Au lieu de cela, nous prédisons les classes et les boîtes englobantes de l'image entière à une seule exécution de l'algorithme et détectons de multiples objets en utilisant un seul réseau neuronal. 	L'algorithme YOLO est rapide par rapport aux autres algorithmes de classification.
	En temps réel, notre algorithme traite 45 images par seconde. L'algorithme YOLO fait des erreurs de localisation mais prédit moins de faux positifs en arrière-plan.

INTRODUCTION 
	Object detection is a technology that detects the semantic objects of a class in digital images and videos. One of its real-time applications is self-driving cars. In this, our task is to detect multiple objects from an image. The most common object to detect in this application is the car, motorcycle, and pedestrian. For locating the objects in the image we use Object Localization and have to locate more than one object in real-time systems. There are various techniques for object detection, they can be split up into two categories, first is the algorithms based on Classifications. CNN and RNN come under this category. In this, we have to select the interested regions from the image and have to classify them using Convolutional Neural Network. This method is very slow because we have to run a prediction for every selected region. The second category is the algorithms based on Regressions. YOLO method comes under this category. In this, we won't select the interested regions from the image. Instead, we predict the classes and bounding boxes of the whole image at a single run of the algorithm and detect multiple objects using a single neural network. YOLO algorithm is fast as compared to other classification algorithms. In real time our algorithm process 45 frames per second. YOLO algorithm makes localization errors but predicts less false positives in the background.
